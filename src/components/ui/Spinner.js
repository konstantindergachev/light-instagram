import React from 'react';
import './Spinner.scss';

const Spinner = () => (
  <section className="spinner__ring-wrapper">
    <div className="spinner__ring" />
  </section>
);

export default Spinner;
